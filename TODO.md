# TODO

## Estado: Pendiente

- [ ] Corregir `perfil.html`: quitar CSS/JS redundante para que no se desordene al abrir `/perfil`.
- [ ] Corregir bindings Thymeleaf inseguros (evitar `usuario?.rol`) para garantizar render correcto.
- [ ] Validar que el layout respete Bootstrap (sin estilos globales propios que compitan con `layout.html`).
